// =================================== ( 1 ) ===================================
const fn1 = (str) => {
    return str;
}

// console.log(fn1('dasturlash.uz'));

// =================================== ( 2 ) ===================================
const fn2 = (str) => {
    return str;
}

// console.log(fn2('dasturlash.uz'));

// =================================== ( 3 ) ===================================
const fn3 = (num, str) => {
    return {
        num, str
    };
}

// console.log(fn3(2, 'dasturlash.uz'));

// =================================== ( 4 ) ===================================
const fn4 = (n1, n2) => {
    return n1 + n2;
}

// console.log(fn4(2, 5));

// =================================== ( 5 ) ===================================
// =================================== ( 6 ) ===================================
// =================================== ( 7 ) ===================================
// =================================== ( 8 ) ===================================
// =================================== ( 9 ) ===================================
// =================================== ( 10 ) ===================================
